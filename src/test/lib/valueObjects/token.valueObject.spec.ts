// import { Token } from '@common';

describe('Token test', () => {
  it('should be true', () => {
    // const token = Token.generate('test', 'userid1', 'prueba@prueba.com');
    // console.log(token);
    expect(true).toBeTruthy();
  });
});
